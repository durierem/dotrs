# frozen_string_literal: true

module Dotrs
  # VERSION = begin
  #   version = `git describe --always --tag 2> /dev/null`.chomp
  #   version = version[1..] if version.start_with?('v') # Remove potential starting 'v'
  #   version
  # rescue StandardError
  #   'N/A'
  # end

  VERSION = '3.0.0-dev'
end
